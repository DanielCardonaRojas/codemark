target/
.DS_Store
# Ignore the local codemark workspace, but track the repo's own knowledge-base
# database — it doubles as the fixture for the TUI screenshot gallery.
.codemark/*
!.codemark/codemark.db
# SQLite sidecar files are transient; never track them.
.codemark/codemark.db-wal
.codemark/codemark.db-shm
*.tar.gz
man/
node_modules/
site/
pack-cache/
data/

.claude
.mcp.json
.gemini/
lat.md/
graphify-out/
dev-docs/

# Codetours Server
codetours.toml

# Environment variables with secrets
.env
.env.local
.env.*.local

# Local docker-compose overrides
docker-compose.override.yml
