import{_ as o,o as t,c as r,a2 as s}from"./chunks/framework.Bqn-bhhR.js";const m=JSON.parse('{"title":"Multi-repo queries in the TUI","description":"","frontmatter":{},"headers":[],"relativePath":"superpowers/specs/2026-08-07-tui-multi-repo-queries-design.md","filePath":"superpowers/specs/2026-08-07-tui-multi-repo-queries-design.md","lastUpdated":1786477483000}'),a={name:"superpowers/specs/2026-08-07-tui-multi-repo-queries-design.md"};function i(n,e,c,d,l,h){return t(),r("div",null,[...e[0]||(e[0]=[s('<h1 id="multi-repo-queries-in-the-tui" tabindex="-1">Multi-repo queries in the TUI <a class="header-anchor" href="#multi-repo-queries-in-the-tui" aria-label="Permalink to &quot;Multi-repo queries in the TUI&quot;">​</a></h1><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p>The TUI browses one repository at a time. Selecting a repo in the Repos panel calls <code>switch_database()</code>, which <em>replaces</em> the single <code>self.db: Database</code> wholesale. The entire browser — panel refresh, right-pane rendering, heal, live-health, search — reads from that one <code>self.db</code> and derives the repo root from <code>self.db.path().parent().parent()</code>.</p><p>The CLI already supports cross-repo queries: <code>codemark list --repo a/b --repo c/d</code> opens several databases and merges the results. Connecting related code across repositories is valuable, but the TUI can&#39;t do it.</p><p>Enabling multi-repo in the TUI raises two UX problems the design must solve:</p><ol><li>Once bookmarks from several repos share one pane, it&#39;s hard to tell which repo a row belongs to.</li><li>Tours are backed by per-repo local collections plus a remote Codetours server scoped to a repo set; merging tours across repos is more involved than it&#39;s worth for a first version.</li></ol><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>Let the user query several repositories at once in the TUI by checking multiple repos in the Repos panel. Merged bookmark/collection rows must show which repo each belongs to. When exactly one repo is checked, behavior is identical to today.</p><h2 id="decisions-settled-during-design" tabindex="-1">Decisions (settled during design) <a class="header-anchor" href="#decisions-settled-during-design" aria-label="Permalink to &quot;Decisions (settled during design)&quot;">​</a></h2><ul><li><strong>Selection:</strong> the Repos panel becomes a multi-select (like the existing Owners panel). Space toggles a repo in/out of the query scope. There is <strong>no single &quot;active&quot; DB</strong> — the query spans every checked repo.</li><li><strong>Row labeling:</strong> when more than one repo is in scope, the bookmark/collection row replaces the <strong>author</strong> field with the <strong>bare repo name</strong> (e.g. <code>codemark</code>). Single-repo mode keeps the author exactly as today.</li><li><strong>Tours:</strong> the Tours tab is <strong>hidden whenever more than one repo is checked</strong>. With exactly one repo checked it works fully (local + remote), unchanged.</li><li><strong>DB architecture:</strong> a stateful, TUI-side workspace owns one open <code>Database</code> per checked repo (approach A). Reuses core primitives; does <strong>not</strong> reuse <code>Workspace::open_all</code> (that resolves CLI flags, not panel selections).</li><li><strong>Core consolidation:</strong> no broad &quot;move CLI logic to core&quot; pass. Only the one genuinely shared, non-trivial helper — <code>find_bookmark_across</code> — moves into core. The fan-out/tagging loops stay in each consumer because their outputs diverge (CLI stdout vs. TUI <code>PanelItem</code>s).</li><li><strong>Semantic search across repos</strong> is the primary use case and does <strong>not exist today</strong> (both the CLI and TUI semantic paths are single-DB). A new core primitive embeds the query <strong>once</strong> and searches each DB&#39;s connection, merging by distance. It is wired into <strong>both</strong> the TUI and the CLI (parity).</li><li><strong>Model comparability:</strong> cross-repo semantic search <strong>assumes all checked repos are indexed with the same embedding model/metric</strong> — no guard for v1. Distances from a differently-indexed repo would rank incorrectly. This is an accepted known limitation; see &quot;Known limitations&quot; for the future guard.</li></ul><h2 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h2><ul><li>No cross-repo <em>tours</em> (Tours is hidden in multi-repo mode).</li><li>No new &quot;create bookmark&quot; flow. TUI writes remain item-scoped (delete, heal, publish/sync of a selected item).</li><li>No embedding-model <strong>compatibility guard</strong> for cross-repo semantic search (v1 assumes a shared model; see Known limitations).</li><li>No general &quot;multi-repo query engine&quot; in core — only the two targeted shared primitives (<code>find_bookmark_across</code>, the embed-once semantic search).</li></ul><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><h3 id="what-core-already-provides-and-what-it-does-not" tabindex="-1">What core already provides (and what it does not) <a class="header-anchor" href="#what-core-already-provides-and-what-it-does-not" aria-label="Permalink to &quot;What core already provides (and what it does not)&quot;">​</a></h3><p><code>crates/codemark-core/src/storage/workspace.rs</code> provides multi-repo <strong>opening/discovery</strong> only:</p><ul><li><code>Workspace::open_all(opts) -&gt; Vec&lt;(String label, Database)&gt;</code> — a list of independent single-repo connections, built once from CLI options.</li><li><code>source_label_from_path</code>, <code>project_root</code>, <code>Database::open</code> — primitives.</li></ul><p>There is <strong>no</strong> cross-repo query/merge API. Every query method (<code>list_bookmarks</code>, <code>search</code>, <code>get_bookmark</code>, …) is a method on a single <code>Database</code>. The CLI does the fan-out itself: iterate the Vec, run the per-DB query, tag each result with the label, and format for stdout.</p><p>The TUI reuses the primitives (<code>Database::open</code>, <code>source_label_from_path</code>, <code>project_root</code>) but <strong>not</strong> <code>open_all</code>, because the TUI&#39;s scope is the set of <em>checked repos in the Repos panel</em> (registry-backed roots), not CLI <code>--db</code>/<code>--repo</code> flags, and it mutates at runtime.</p><h3 id="step-0-—-shared-core-primitives" tabindex="-1">Step 0 — shared core primitives <a class="header-anchor" href="#step-0-—-shared-core-primitives" aria-label="Permalink to &quot;Step 0 — shared core primitives&quot;">​</a></h3><p>Two targeted extractions/additions into <code>codemark-core</code>, done first so both the CLI and TUI consume them:</p><p><strong>0a. Relocate <code>find_bookmark_across</code>.</strong> Move it (currently <code>crates/codemark-cli/src/cli/handlers.rs</code>) into <code>codemark-core</code> (e.g. <code>Workspace::find_bookmark_across(dbs, id) -&gt; Result&lt;(Bookmark, &amp;Database)&gt;</code>, matching full-id then ≥4-char prefix). The CLI calls the relocated version so its tests stay green; the TUI reuses it for open-by-id and resolving a selected row&#39;s DB. Everything else in the CLI stays put. The two DB filters (<code>filter_dbs_by_user_email</code>, <code>filter_dbs_by_repo_owner</code>) may move too if convenient — optional, not required.</p><p><strong>0b. Embed-once semantic primitive.</strong> Today <code>SemanticRepo::search(conn, query, limit)</code> rebuilds the embedding provider (loads the model) and re-embeds the query on every call — so looping it over N repos would load the model N times. Split embedding from per-connection search:</p><ul><li><code>SemanticRepo::embed_query(query) -&gt; Result&lt;Vec&lt;f32&gt;&gt;</code> — builds the provider once and returns the query embedding.</li><li><code>SemanticRepo::search_prepared(conn, &amp;embedding, limit, threshold) -&gt; Result&lt;Vec&lt;SearchResult&gt;&gt;</code> — runs <code>VecStore::search_with_threshold</code> against a single connection using a pre-computed embedding (no model load). <code>VecStore::search_with_threshold</code> already accepts a <code>&amp;[f32]</code>, so this is a clean extraction. Add the collection-target variant too (<code>search_collections_prepared</code>).</li></ul><p>The existing <code>search</code>/<code>search_collections</code> become thin wrappers (<code>embed_query</code> + <code>search_prepared</code>) so current single-DB callers are unchanged.</p><h3 id="component-1-—-repoworkspace-new-tui-side" tabindex="-1">Component 1 — <code>RepoWorkspace</code> (new, TUI-side) <a class="header-anchor" href="#component-1-—-repoworkspace-new-tui-side" aria-label="Permalink to &quot;Component 1 — `RepoWorkspace` (new, TUI-side)&quot;">​</a></h3><p>A small stateful owning container that replaces <code>self.db</code>:</p><ul><li>Owns the open <code>Database</code> for each <strong>checked</strong> repo, keyed by repo root, plus a <code>focus</code> repo root. Container type (Vec vs. IndexMap) is an implementation detail — at ≤50 repos a linear scan is fine; what matters is that it is <em>stateful</em> (opens/drops on toggle) and supports <em>lookup by repo root</em>.</li><li><code>set_scope(checked_roots)</code>: opens any newly-checked DB (<code>&lt;root&gt;/.codemark/codemark.db</code>) and drops any no-longer-checked DB. Does <strong>not</strong> reopen unchanged connections — toggling one repo must not reopen the rest (avoids nav-path lag).</li><li><code>dbs()</code> (ordered iteration), <code>get(root) -&gt; Option&lt;&amp;Database&gt;</code>, <code>focus_db() -&gt; &amp;Database</code>, <code>is_multi() -&gt; bool</code>.</li><li><strong>Invariant:</strong> at least one repo is always checked. Attempting to uncheck the last checked repo is a no-op.</li><li><code>focus</code> = the row under the cursor if it is checked, else the first checked repo. Used <strong>only</strong> for: config/server/token resolution (remote tours, sync), single-repo Tours, and the default preview when nothing is selected. In single-repo mode <code>focus_db()</code> is the one DB, so those paths are unchanged.</li></ul><p>Startup seeds the scope with the auto-detected current repo checked — identical to today&#39;s single-repo default.</p><h3 id="component-2-—-merged-row-building" tabindex="-1">Component 2 — merged row building <a class="header-anchor" href="#component-2-—-merged-row-building" aria-label="Permalink to &quot;Component 2 — merged row building&quot;">​</a></h3><p><code>build_content_items(db, …)</code> stays per-DB and unchanged. A new merge layer:</p><ul><li>iterates <code>workspace.dbs()</code>, calls the existing per-DB builder for each, tags every produced <code>PanelItem</code> with its repo (name + root), and concatenates.</li><li>sorts the merged list by <code>created_at</code> descending, so repos interleave by recency rather than clumping by repo.</li><li>when <code>workspace.is_multi()</code>, replaces the row&#39;s author/metadata field with the bare repo name. In single-repo mode the author is left exactly as today.</li></ul><p>Collections get the same treatment (repo tag + author→repo swap in multi mode).</p><p><strong>Row repo identity.</strong> Each row must carry its repo so selection, live-health caching, and per-item DB lookup can&#39;t collide on ids shared across repos. The encoding (a dedicated <code>repo</code> field on <code>PanelItem</code> vs. encoding the root into <code>user_data</code>) is deferred to implementation. Whatever is chosen must key: selection restoration, the live-health cache, and <code>db_for_item</code> by <code>(repo_root, id)</code> rather than bare <code>id</code>.</p><h3 id="component-3-—-per-item-db-resolution" tabindex="-1">Component 3 — per-item DB resolution <a class="header-anchor" href="#component-3-—-per-item-db-resolution" aria-label="Permalink to &quot;Component 3 — per-item DB resolution&quot;">​</a></h3><p>A helper <code>db_for_item(item) -&gt; &amp;Database</code> looks up the item&#39;s repo root in the workspace. It is threaded into every path that currently assumes <code>self.db</code>:</p><ul><li>right-pane preview (<code>load_bookmark_live</code> / <code>load_tour_live</code>), using the item&#39;s repo root as the markdown <code>repo_path</code>;</li><li>heal target;</li><li>delete (including the collection-cascade bookmark count);</li><li>live-health apply;</li><li>open-by-id / search-result open (via the relocated <code>find_bookmark_across</code>).</li></ul><h3 id="component-4-—-live-health-caches" tabindex="-1">Component 4 — live-health caches <a class="header-anchor" href="#component-4-—-live-health-caches" aria-label="Permalink to &quot;Component 4 — live-health caches&quot;">​</a></h3><p>The bookmark and collection live-health <code>HashMap</code>s become keyed by <code>(repo_root, id)</code> instead of bare <code>id</code>, so colliding ids across repos don&#39;t cross-contaminate. The existing generation guard is unchanged. <code>spawn_live_health_task</code> fans out across all checked repos.</p><h3 id="component-5-—-search-fts-and-semantic" tabindex="-1">Component 5 — search (FTS and semantic) <a class="header-anchor" href="#component-5-—-search-fts-and-semantic" aria-label="Permalink to &quot;Component 5 — search (FTS and semantic)&quot;">​</a></h3><p>Search fans out across all checked DBs and merges results, each tagged by repo. The existing <code>request_id</code> / generation guards are preserved (a merged result set belongs to one request_id). Results carry the repo so opening one resolves the correct DB.</p><p><strong>FTS.</strong> <code>Database::search_bookmarks</code> is a per-DB <code>LIKE</code> scan with no global score, so the merge is a concatenation. The TUI&#39;s <code>execute_bookmark_search</code> FTS arm (and the collection equivalent) loops <code>workspace.dbs()</code>, runs the per-DB search, tags each row with its repo, and concatenates. It stays synchronous.</p><p><strong>Semantic (primary use case).</strong> Built on the Step 0b primitive so the model loads once per query, not once per repo:</p><ol><li>Resolve model/metric/threshold <strong>once</strong> from the <strong>focus repo&#39;s</strong> config (<code>Config::load_layered</code>). Per the model-comparability decision, this single config is applied to all checked repos (v1 assumes a shared model).</li><li><code>embed_query(query)</code> once.</li><li>For each checked DB: <code>search_prepared(db.conn(), &amp;embedding, limit, threshold)</code>, tagging each <code>SearchResult</code> with its repo. A repo with no vec index / no embeddings simply returns nothing.</li><li>Merge all hits, <strong>global sort by distance ascending, truncate to <code>SEARCH_RESULT_LIMIT</code></strong>, then resolve each surviving hit to its full bookmark from its own DB.</li></ol><p>This runs on the existing blocking task (model load + embed). The same embed- once/merge flow applies to collection semantic search.</p><p><strong>CLI parity.</strong> <code>handle_semantic_search</code> (currently <code>open_db</code> single-DB) is rewired to <code>open_all_dbs*</code> + the same embed-once/merge flow, so <code>codemark search --semantic --repo a/b --repo c/d</code> works and matches the TUI. FTS CLI search already fans out and is unchanged.</p><h3 id="component-6-—-tours-visibility" tabindex="-1">Component 6 — Tours visibility <a class="header-anchor" href="#component-6-—-tours-visibility" aria-label="Permalink to &quot;Component 6 — Tours visibility&quot;">​</a></h3><p><code>update_tours_tab_visibility</code> additionally hides the Tours tab whenever <code>workspace.is_multi()</code>. With exactly one repo checked, Tours works fully (local + remote) as today. The remote-tour fetch already scopes by the active repos in the Repos panel, so no change is needed beyond the visibility gate.</p><h3 id="component-7-—-selection-flow" tabindex="-1">Component 7 — selection flow <a class="header-anchor" href="#component-7-—-selection-flow" aria-label="Permalink to &quot;Component 7 — selection flow&quot;">​</a></h3><p>For the Repos tab, <code>activate_context_selection</code> toggles the item (multi-select, like Owners) and calls <code>workspace.set_scope(checked_roots)</code> followed by <code>refresh_all_panels</code>, replacing today&#39;s <code>switch_database</code> + focus-move. The <code>switch_database</code> method is removed (or reduced to the seed-one-repo path).</p><h2 id="data-flow" tabindex="-1">Data flow <a class="header-anchor" href="#data-flow" aria-label="Permalink to &quot;Data flow&quot;">​</a></h2><ol><li>User toggles a repo in the Repos panel → <code>set_scope</code> opens/drops DBs → panels refresh.</li><li>Refresh merges rows across <code>workspace.dbs()</code>, tags each with its repo, swaps author→repo when <code>is_multi()</code>, sorts by recency.</li><li>Selecting a row → <code>db_for_item</code> resolves the owning DB → right pane renders from that DB with the item&#39;s repo root as <code>repo_path</code>.</li><li>Heal / delete / live-health operate through the resolved per-item DB.</li><li><code>is_multi()</code> gates Tours-tab visibility.</li></ol><h2 id="edge-cases" tabindex="-1">Edge cases <a class="header-anchor" href="#edge-cases" aria-label="Permalink to &quot;Edge cases&quot;">​</a></h2><ul><li><strong>Uncheck-last</strong> is a no-op; the scope never empties.</li><li><strong>Owner filter hides a checked repo:</strong> it stays in scope (it can&#39;t be unchecked while hidden). Minor, accepted for v1.</li><li><strong>Empty repos</strong> (no bookmarks/collections) merge cleanly — they contribute nothing.</li><li><strong>Id collision across repos</strong> is handled by keying selection, live-health, and <code>db_for_item</code> on <code>(repo_root, id)</code>.</li><li><strong>Repo without a semantic index</strong> contributes no semantic hits (its <code>search_prepared</code> returns empty); the other repos still return results.</li></ul><h2 id="known-limitations" tabindex="-1">Known limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known limitations&quot;">​</a></h2><ul><li><strong>Cross-repo semantic ranking assumes a shared embedding model/metric.</strong> v1 applies one model (from the focus repo) to every checked repo and merges raw distances. If a repo was indexed with a different model or dimension, its distances are not comparable and it will rank incorrectly (or, on a dimension mismatch, the underlying <code>VecStore</code> query may error for that DB). A future guard could resolve each repo&#39;s configured model and either skip or warn on mismatch (the vec-table dimension is the reliable signal; per-embedding model identity is not currently stored). Out of scope for v1 by decision.</li></ul><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h2><p><strong>Unit</strong></p><ul><li><code>RepoWorkspace</code>: <code>set_scope</code> opens newly-checked and drops unchecked without reopening unchanged DBs; uncheck-last is a no-op; <code>focus_db</code> resolves per the cursor/first-checked rule.</li><li>Merged builder: tags each row with its repo; swaps author→repo when multi; exactly-one-checked output is identical to today (parity test).</li><li><code>db_for_item</code> resolves the correct DB by repo root.</li><li>Tours visibility flips with <code>is_multi()</code>.</li><li>Relocated <code>find_bookmark_across</code> still matches full id and ≥4-char prefix.</li><li><code>SemanticRepo::embed_query</code> + <code>search_prepared</code> produce the same results as the old <code>search</code> for a single DB (refactor-parity), and <code>search_prepared</code> runs without reloading the model.</li></ul><p><strong>Integration</strong></p><ul><li>Two temp repos, each with bookmarks → check both → merged list shows both, each labeled with its repo name.</li><li>Select a repo-B bookmark → preview, heal, and delete all target repo B.</li><li>FTS search across both repos merges and tags results by repo.</li><li><strong>Semantic search across both repos</strong>: index both, run one semantic query, assert hits from both repos appear, ordered by ascending distance, capped at the limit — and the embedding model is loaded once (not once per repo).</li><li>CLI regression: <code>find_bookmark_across</code> relocation keeps existing CLI tests green; <code>codemark search --semantic</code> with multiple <code>--repo</code> returns merged, distance-ordered hits.</li></ul>',60)])])}const u=o(a,[["render",i]]);export{m as __pageData,u as default};
